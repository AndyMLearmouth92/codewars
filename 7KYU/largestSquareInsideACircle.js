/* Determine the area of the largest square that can fit inside a circle with radius r.

Starter code:

function areaLargestSquare(r) {
  return ; //code here
}
*/

// Solution:

const areaLargestSquare = (r) => Math.pow(r, 2) * 2;
